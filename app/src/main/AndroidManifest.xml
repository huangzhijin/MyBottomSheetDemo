<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zcp.coordinatorlayoutdemo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme2">
        <activity
            android:name=".activity.BottomSheetActivityDemo"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.BottomSheetActivity" />
        <activity android:name=".activity.MyBehaviorActivity" />
        <!-- <activity android:name=".activity.AppBarActivityDemo"></activity> -->
        <!-- <activity android:name=".activity.DrawerLayoutActivity"></activity> -->
        <!-- <activity android:name=".activity.DrawerLayoutSecondActivity"></activity> -->
        <!-- <activity android:name=".activity.BottomNavigationBarActivity"></activity> -->
<!--
        <activity android:name=".activity.BottomSheetActivityDemo"></activity>
-->
    </application>

</manifest>